# CLI Resume

Terminal-inspired resume with cinematic intro and AI chatbot.
Zero dependencies. One file. Fully static.


## Quick Start

1. **Fork** this repository
2. **Open** `setup.html` in your browser
3. **Paste** your resume text + free Gemini API key
4. **Answer** 20 common interview questions (in your own words)
5. **Deploy** directly to your repo — or download `data.json`
6. **Enable GitHub Pages**: Settings → Pages → main branch → Save
7. **Share** your link: `https://username.github.io/cli-resume`

### Get a Gemini API Key (free)
1. Go to https://aistudio.google.com/apikey
2. Click "Create API Key"
3. Copy the key — that's it

### Manual Setup
Prefer to skip the wizard? Copy `data.example.json` to `data.json` and edit it manually.

## Customization
- **Photo**: Add `photo.jpg` to the repo root
- **Colors**: Edit CSS variables in `index.html`
- **Sections**: Modify `data.json` fields

## How It Works
- `index.html` — The resume template (HTML + CSS + JS in one file)
- `setup.html` — AI-powered setup wizard (parses resume, generates data)
- `data.json` — Your resume data (generated by setup, gitignored)
- `data.example.json` — Schema reference

## License
MIT
